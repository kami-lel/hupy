# Paper Trail (PT) Documentation

**Paper Trail (PT)** requires that certain files were actually touched alongside a commit — a changelog entry alongside a feature landing, a migration file alongside a schema change — so the paperwork can't quietly slip.
